{
  "name": "mido-xi-capture",
  "version": "0.1.0",
  "private": true,
  "description": "MIDO XI Capture — Chrome extension. Save football moments from YouTube to the MIDO XI Player OS.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "package": "node build.mjs --zip"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5"
  }
}
